// Helper configuration to connect the frontend to the local backend.
// IMPORTANT: To run this app on a physical device using Expo Go, 
// replace 'localhost' or '127.0.0.1' below with your computer's local Wi-Fi IP address.
// Example: http://192.168.1.50:5000/api

const config = {
  // Swap localhost with your local network IP (e.g., 192.168.X.X) for physical devices
  API_BASE_URL: 'http://192.168.1.100:5000/api', 
};

export default config;
